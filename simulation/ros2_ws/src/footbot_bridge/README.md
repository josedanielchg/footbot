# footbot_bridge

Compatibility bridge package for the Footbot simulation.

Future contents:

- HTTP-compatible simulation adapter
- Possible `/move` compatibility endpoint
- Translation between existing manual control commands and ROS topics

Not implemented yet:

- HTTP server
- `/move` endpoint
- Manual-control migration
