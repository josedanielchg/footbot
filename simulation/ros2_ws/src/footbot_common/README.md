# footbot_common

Shared constants and small utilities for the FootBot simulation.

This package intentionally contains no ROS nodes. It provides canonical topic
names, frame names, approximate geometry constants, and tiny math helpers that
can be reused by behavior, perception, and documentation-oriented code.
